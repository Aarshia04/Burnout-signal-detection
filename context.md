# Interaction Context

This document summarizes the current state of the workspace, recent
changes, and the refactoring carried out during the conversation.

## Workspace Structure (after refactor)

```
burnout_detection/
├── data/                     # generated or loaded datasets
├── docs/                     # methodology and To-be-addressed notes
├── models/                   # trained models & encoders
├── notebooks/                # analysis notebook
├── src/
│   ├── data_loader.py        # loads synthetic or OULAD data
│   ├── feature_engineering.py# aggregation & derived features
│   ├── generate_dataset.py   # original synthetic generator
│   ├── predict.py            # prediction utilities
│   └── run_analysis.py       # CLI driver for full pipeline
├── requirements.txt
├── README.md
├── PROJECT_SUMMARY.md
└── context.md                # <-- this file
```

## Key Refactorings

* Added support for real dataset (Open University Learning Analytics,
  aka OULAD) alongside the synthetic data.
* Created `data_loader` and `feature_engineering` modules to centralize
  loading and feature preparation logic.
* Extended `run_analysis.py` with CLI flags, encoder persistence, and
  dataset agnostic behavior.
* Updated `predict.py` to load multiple label encoders and apply them by
  column name.
* Patched notebook to detect encoding availability and to optionally use
  shared helper functions.
* Updated README, PROJECT_SUMMARY, and methodology to document OU dataset
  usage and to moderate claims about synthetic accuracy.

## Recent Commands

* `python3 src/run_analysis.py --data-type synthetic --n-students 100 --n-weeks 4`
  (failed due to missing target column error) – indicates bug in synthetic
  path or feature preparation logic.

## Outstanding Issues

* The error during test run: "Target column 'burnout_status' not found in
  data" when using small synthetic dataset. Needs investigation / fix.

## Next Steps

1. Investigate and correct the target column handling in
   `run_analysis`/`feature_engineering`.
2. Optionally test the pipeline with OULAD data if available.
3. Commit changes to git as needed.

---

*Generated by Copilot at user's request.*